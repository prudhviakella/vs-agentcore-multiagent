"""
shared/a2a/client.py
====================
A2A HTTP client — used by Supervisor to discover agents and send tasks.

TODO: implement
  - fetch_agent_card(base_url) -> AgentCard
  - send_task(agent_url, message, session_id) -> TaskResult
  - send_tasks_parallel(tasks[]) -> list[TaskResult]
"""
